import { Flag } from "../models/Flag"

//having issues with this section 
export class AssociateDTO{
    email!: string
    salesforceId!: string
    firstName!: string
    lastName!: string
    flag!: Flag
}